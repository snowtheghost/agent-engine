provider:
  name: claude
  model: null
  options:
    timezone: UTC
    max_buffer_size: 100000000
    betas: []

vault:
  # directory: /path/to/vault  # defaults to data_dir

discord:
  token: null
  channel_id: null
  character_limit: 2000
  history_limit: 50

http:
  host: 127.0.0.1
  port: 8938
  enabled: true

log_level: INFO
